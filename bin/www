#!/usr/bin/env node

/**
 * Module dependencies.
 */

var app = require("../app");
// var debug = require("debug")("insurance-data-managment:server");
// var http = require("http");

const port = process.env.PORT || "3000";
app.listen(port, () => {
  console.log(`server running on port ${port}`);
});
